Готов. Вот **полноценный, структурный, современный README** в корпоративном стиле под твой проект **geneResume** — как будто ты выкатываешь продукт на GitHub для работодателя или заказчика.

Готово к прямому копированию.

---

# geneResume — Генератор Резюме

Современный веб-сервис для создания структурированных резюме с удобным интерфейсом, экспортом в PDF и поддержкой различных шаблонов. Проект разработан как лёгкое, производительное SPA на основе React + Vite.

---

## 🚀 Демонстрация

**Live Demo:** [https://gene-resume.vercel.app/](https://gene-resume.vercel.app/)
**GitHub:** [https://github.com/Kutman02/geneResume.git](https://github.com/Kutman02/geneResume.git)

---

## 📦 Технологический стек

Проект построен на современной фронтенд-архитектуре:

* **React** — компонентный UI и state-driven логика
* **Vite** — ультра-быстрая сборка и dev-сервер
* **TypeScript** — строгая типизация и стабильность кода
* **Redux Toolkit** — глобальное состояние, slice-архитектура
* **React Router** — маршрутизация страниц
* **TailwindCSS** — атомарные стили и адаптивность
* **RTK Query / Axios** — сетевые запросы и обработка данных
* **React Hook Form + Zod** — формы, валидация, UX без ошибок
* **html2pdf.js** — экспорт данных профиля в PDF

---

## 🎯 Ключевые возможности (Features)

* **Создание резюме с нуля**
  Пользователь заполняет личные данные, опыт, навыки, образование.

* **Готовые шаблоны оформления**
  Единый дизайн с поддержкой адаптивных макетов.

* **Живой предпросмотр резюме**
  Любые изменения отображаются мгновенно.

* **Экспорт в PDF без потери стилей**
  Реализован кастомный рендер, инлайн-стили, оптимизация перед экспортом.

* **Редактирование всех блоков**
  Полная кастомизация: блоки, цвет, шрифт, структура.

* **Сохранение данных пользователя**
  Используется Redux Toolkit + localStorage для сохранения состояния.

* **Адаптивный интерфейс**
  Корректная работа на телефоне, планшете и десктопе.

* **Маршрутизация страниц**
  Пошаговое создание резюме: ввод данных → предпросмотр → экспорт.

---

## 🧩 Архитектура проекта

```
src/
 ├── components/     // UI-компоненты
 ├── pages/          // Страницы маршрутов
 ├── store/          // Redux Toolkit (slices)
 ├── hooks/          // Кастомные хуки
 ├── utils/          // Вспомогательные функции (PDF, конвертация)
 ├── styles/         // Tailwind + кастомные глобальные стили
 └── types/          // Интерфейсы TypeScript
```

Логика разбита по slices:

* **resumeSlice** — структура данных резюме
* **uiSlice** — состояния UI, модальные окна, шаги и т.д.

---

## 📥 Установка и запуск

```bash
git clone https://github.com/Kutman02/geneResume.git
cd geneResume
npm install
npm run dev
```

Сборка для продакшена:

```bash
npm run build
```

---

## 🛠️ Команды

| Команда           | Описание                    |
| ----------------- | --------------------------- |
| `npm run dev`     | Локальный dev-сервер (Vite) |
| `npm run build`   | Продакшен-сборка            |
| `npm run preview` | Просмотр продакшен-билда    |

---

## 📄 Экспорт PDF

Реализован собственный модуль `exportToPdf.ts`, который:

* клонирует DOM-узлы
* конвертирует стили в inline-формат
* очищает мусорные классы
* рендерит чистую структуру в PDF
* сохраняет файл локально

Это помогает избежать типичных багов html2pdf.

---

## 🤝 Контрибьютинг

Pull-requests приветствуются.
Если видишь в проекте улучшения — создавай issue или PR.

---

## 📜 Лицензия

MIT — свободное использование, модификация, распространение.

---

Если хочешь — могу:

🔹 кастомизировать README под стиль твоего GitHub
🔹 добавить скриншоты
🔹 написать changelog
🔹 описать архитектуру ещё глубже (с примерами slices/компонентов)
🔹 подготовить README на английском

Скажи — сделаю.
